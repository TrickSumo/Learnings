Learning Outcomes:-

* REACT elements are JSON objects

* REACT core is responsible for creatinbg element and diff algorithm. Renderers (ReactDOM and REACt Native) are responsible for actual rendering.

* In React.createElement, children can be nested and siblings can be passed as list.